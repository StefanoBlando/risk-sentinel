# RiskSentinel — 2-Minute Demo Script

## 0:00 - 0:15 | Hook
"What happens if JPM crashes 40% tomorrow?"

Open RiskSentinel and explain that it simulates contagion over 210 S&P 500 stocks.

## 0:15 - 0:45 | Deterministic Engine
Run: `What if JPM crashes 40% on 2025-12-01?`

Narration:
- Network/regime context is loaded.
- Shock propagates wave-by-wave.
- We immediately get affected nodes, defaults, and stress metrics.

Alternative (faster live demo): click `Run Full Agentic Demo` in sidebar.

## 0:45 - 1:20 | Agentic Control Plane
Run complex query: compare JPM vs GS with hedge recommendation.

Narration:
- Planner sets bounded plan.
- Architect + Quant run in parallel.
- Advisor synthesizes strict JSON.
- Critic validates consistency against deterministic evidence.

## 1:20 - 1:45 | Explainability + Judge KPIs
Open Explainability panel:
- Deterministic facts injected.
- Evidence-RAG references (`R1..`).
- Planner/Executor/Critic verdict badges.
- Judge Dashboard: critic pass-rate, factual consistency, p95 latency, fallback rate.

## 1:45 - 2:00 | Close
Show export bundle and conclude:
"RiskSentinel delivers fast, auditable systemic-risk intelligence with robust fallback behavior for real demo conditions."

## Backup Plan (if GPT unavailable)
- Keep `Use GPT Orchestrator` ON but rely on automatic deterministic fallback.
- Show that local simulation still returns complete quantitative outputs.
- Highlight resilience as a feature, not a failure.
